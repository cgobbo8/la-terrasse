---
/**
 * ProducerCard — displays a local producer portrait with story.
 * Brun terre pole identity. Pure Astro, zero JS.
 * Design: image-forward card matching site DA (rounded-2xl, shadow-sm).
 */
interface Props {
  name: string;
  photo: string;
  product: string;
  story: string;
}

const { name, photo, product, story } = Astro.props;
---

<article class="rounded-2xl overflow-hidden bg-white shadow-sm">
  <div class="aspect-[4/3] overflow-hidden">
    <img
      src={photo}
      alt={`${name}, ${product}`}
      loading="lazy"
      class="w-full h-full object-cover"
    />
  </div>
  <div class="p-6">
    <h3 class="font-heading text-xl font-bold" style="color: #2D2B1B">
      {name}
    </h3>
    <p class="text-sm font-medium uppercase tracking-wider mt-1" style="color: #2D2B1B99">
      {product}
    </p>
    <p class="text-sm text-gray-600 mt-3 leading-relaxed">
      {story}
    </p>
  </div>
</article>
