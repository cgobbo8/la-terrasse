---
/**
 * LA SALLE — SÉMINAIRES & PRO
 * Dedicated page for seminar packages (Simple, Gourmet, Aventure)
 * CTA: mailto for quote requests
 */
import BaseLayout from '@/layouts/BaseLayout.astro';
import SectionHero from '@/components/common/SectionHero.astro';
import PackComparator from '@/components/salle/PackComparator.astro';
import SeminarConfigurator from '@/components/salle/SeminarConfigurator.svelte';
import VenueSpecs from '@/components/salle/VenueSpecs.astro';
import CrossLinkBlock from '@/components/common/CrossLinkBlock.astro';
import { Icon } from 'astro-icon/components';
import { getLangFromUrl, useTranslations, getLocalizedPath } from '@/i18n/utils';
import { defaultLang } from '@/i18n/translations';
import { buildQuoteMailto } from '@/lib/pole-config';
import { createReader } from '@keystatic/core/reader';
import keystaticConfig from '../../../keystatic.config';

const lang = getLangFromUrl(Astro.url, Astro.currentLocale);
const t = useTranslations(lang);

// Load seminar packages + venue data from Keystatic
const reader = createReader(process.cwd(), keystaticConfig);
const allSeminars = await reader.collections.seminars.all();
const venueData = await reader.singletons.venue.read();
const capacitySeated = venueData?.capacitySeated ?? 0;
const capacityStanding = venueData?.capacityStanding ?? 0;
const surfaceM2 = venueData?.surfaceM2 ?? 0;
const venueAny = venueData as Record<string, unknown> | null;
const equipment = venueData
  ? (lang !== defaultLang && venueAny?.[`equipment_${lang}`] as readonly string[]) || venueData.equipment || []
  : [] as readonly string[];
const layoutOptions = venueData
  ? (lang !== defaultLang && venueAny?.[`layoutOptions_${lang}`] as readonly { name: string; capacity: number }[]) || venueData.layoutOptions || []
  : [] as readonly { name: string; capacity: number }[];
const seminars = allSeminars
  .filter((s) => s.entry.visible)
  .sort((a, b) => (a.entry.order ?? 0) - (b.entry.order ?? 0));

const packages = seminars.map((s) => ({
  slug: s.slug,
  title: s.entry.title,
  subtitle: s.entry.subtitle ?? '',
  description: s.entry.description ?? '',
  includes: [...(s.entry.includes ?? [])],
  priceFrom: s.entry.priceFrom ?? 'Sur devis',
  features: s.entry.features,
  ctaHref: buildQuoteMailto(s.entry.title),
  highlighted: false,
}));

const seoTitle = 'Séminaire nature près de Toulouse — La Terrasse Saint-Ferréol';
const seoDescription = 'Formules séminaire au lac de Saint-Ferréol : salle équipée, restauration et team building. 3 packs pour entreprises, écoles et institutions.';
---

<BaseLayout
  title={seoTitle}
  description={seoDescription}
  pole="salle"
  pagePath="/la-salle/seminaires"
>
  <!-- Hero -->
  <SectionHero
    tagline={t('salle.seminaires.hero.tagline')}
    subtitle={t('salle.seminaires.hero.subtitle')}
    pole="salle"
  >
    <a href={buildQuoteMailto()} class="bg-white text-bleu-ardoise px-6 py-3 rounded-lg font-medium hover:bg-white/90 transition-colors">
      {t('common.requestQuote')}
    </a>
  </SectionHero>

  <!-- L'ESPACE — Venue specs -->
  <div class="pt-(--spacing-section-mobile) lg:pt-(--spacing-section)">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="font-heading text-2xl font-bold mb-2" style="color: #3d4969;">
        {t('salle.venue.title')}
      </h2>
    </div>
    <VenueSpecs
      capacitySeated={capacitySeated}
      capacityStanding={capacityStanding}
      surfaceM2={surfaceM2}
      equipment={equipment}
      layoutOptions={layoutOptions}
      lang={lang}
    />
  </div>

  <!-- CONFIGURATEUR — Interactive pricing builder -->
  <section class="py-(--spacing-section-mobile) lg:py-(--spacing-section) bg-offwhite">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-10">
        <h2 class="font-heading text-2xl lg:text-3xl font-bold mb-3" style="color: #3d4969;">
          Composez votre séminaire
        </h2>
        <p class="text-gray-500 max-w-xl mx-auto text-sm leading-relaxed">
          Sélectionnez vos options et obtenez une estimation instantanée. Nous vous envoyons votre devis sous 24h.
        </p>
      </div>
      <SeminarConfigurator
        accentColor="#3d4969"
        quoteMailto={buildQuoteMailto('Séminaire')}
        client:visible
      />
    </div>
  </section>

  <!-- TEAM BUILDING -->
  <section class="py-(--spacing-section-mobile) lg:py-(--spacing-section)">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="font-heading text-2xl lg:text-3xl font-bold mb-4" style="color: #3d4969;">
          Team building
        </h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Renforcez la cohésion de votre équipe avec nos activités sur site et nos programmes encadrés par un coach professionnel.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <!-- Activités du centre -->
        <div class="rounded-2xl border border-gray-200 bg-white p-8">
          <div class="flex items-center gap-3 mb-6">
            <span class="w-12 h-12 rounded-xl flex items-center justify-center" style="background-color: #eef5ec;">
              <Icon name="lucide:trees" class="w-6 h-6 text-aventure" />
            </span>
            <div>
              <h3 class="font-heading text-lg font-bold" style="color: #3d4969;">Activités plein air</h3>
              <p class="text-sm text-gray-500">En autonomie, sur le site</p>
            </div>
          </div>
          <ul class="space-y-3">
            {[
              { icon: 'lucide:target', label: 'Archery Tag', desc: 'Stratégie et esprit d\'équipe' },
              { icon: 'lucide:waves', label: 'Paddle & Canoë', desc: 'Cohésion sur le lac' },
              { icon: 'lucide:bike', label: 'VTT', desc: 'Exploration nature en groupe' },
              { icon: 'lucide:compass', label: 'Course d\'orientation', desc: 'Logique et communication' },
              { icon: 'lucide:anchor', label: 'Pédalo', desc: 'Détente et convivialité' },
            ].map((item) => (
              <li class="flex items-center gap-3 text-sm">
                <Icon name={item.icon} class="w-4 h-4 text-aventure flex-shrink-0" />
                <span class="font-medium text-gray-800">{item.label}</span>
                <span class="text-gray-400">—</span>
                <span class="text-gray-500">{item.desc}</span>
              </li>
            ))}
          </ul>
          <a
            href={getLocalizedPath('/aventure', lang)}
            class="inline-flex items-center gap-1 mt-6 text-sm font-medium text-aventure hover:underline"
          >
            Voir toutes les activités
            <Icon name="lucide:arrow-right" class="w-4 h-4" />
          </a>
        </div>

        <!-- Programme coach -->
        <div class="rounded-2xl border-2 p-8 relative overflow-hidden" style="border-color: #3d4969; background: linear-gradient(135deg, #edf0f5 0%, white 100%);">
          <div class="absolute top-4 right-4">
            <span class="inline-flex items-center gap-1 text-xs font-bold uppercase tracking-wider px-3 py-1 rounded-full text-white" style="background-color: #3d4969;">
              <Icon name="lucide:sparkles" class="w-3 h-3" />
              Premium
            </span>
          </div>
          <div class="flex items-center gap-3 mb-6">
            <span class="w-12 h-12 rounded-xl flex items-center justify-center" style="background-color: #3d496915;">
              <Icon name="lucide:trophy" class="w-6 h-6" style="color: #3d4969;" />
            </span>
            <div>
              <h3 class="font-heading text-lg font-bold" style="color: #3d4969;">Coaching professionnel</h3>
              <p class="text-sm text-gray-500">Encadré par un coach spécialisé</p>
            </div>
          </div>
          <ul class="space-y-3">
            {[
              { icon: 'lucide:flame', label: 'Épreuves type Koh-Lanta', desc: 'Dépassement de soi' },
              { icon: 'lucide:brain', label: 'Préparation mentale', desc: 'Gestion du stress et focus' },
              { icon: 'lucide:users', label: 'Cohésion d\'équipe', desc: 'Exercices de confiance et communication' },
              { icon: 'lucide:puzzle', label: 'Épreuves de logique', desc: 'Résolution de problèmes en équipe' },
              { icon: 'lucide:medal', label: 'Défis sportifs', desc: 'Parcours et épreuves physiques' },
            ].map((item) => (
              <li class="flex items-center gap-3 text-sm">
                <Icon name={item.icon} class="w-4 h-4 flex-shrink-0" style="color: #3d4969;" />
                <span class="font-medium text-gray-800">{item.label}</span>
                <span class="text-gray-400">—</span>
                <span class="text-gray-500">{item.desc}</span>
              </li>
            ))}
          </ul>
          <a
            href={buildQuoteMailto('Team building avec coach')}
            class="inline-flex items-center gap-2 mt-6 text-sm font-medium text-white px-5 py-2.5 rounded-lg transition-opacity hover:opacity-90"
            style="background-color: #3d4969;"
          >
            Demander un devis
            <Icon name="lucide:arrow-right" class="w-4 h-4" />
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Section contact -->
  <section class="py-(--spacing-section-mobile) lg:py-(--spacing-section) bg-offwhite">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <p class="text-gray-500 italic mb-4">
        {t('common.requestQuoteDesc')}
      </p>
      <a href={buildQuoteMailto()} class="inline-block px-6 py-3 rounded-lg font-medium transition-colors text-white" style="background-color: #3d4969;">
        {t('common.requestQuote')}
      </a>
    </div>
  </section>

  <!-- Cross-linking structurel -->
  <CrossLinkBlock currentPole="salle" />
</BaseLayout>
